[tool.poetry]
name = "embedding-service"
version = "1.0.0"
description = "ML embedding service for Vibe Search"
authors = ["Your Name"]

[tool.black]
line-length = 100
target-version = ['py311']

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]







